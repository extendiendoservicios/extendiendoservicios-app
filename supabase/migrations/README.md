# `supabase/migrations`

Migraciones numeradas (`0001_...sql`, `0002_...sql`, ...) según el orden de
`04_Modelo_de_Datos.md` sección 11, un bloque por archivo. Una vez aplicada una migración en
`App_dev` no se edita más: un cambio posterior es una migración nueva.

`pnpm db:push` (`supabase db push`, proyecto vinculado) las aplica contra `App_dev`. Nunca contra
`App` (producción) salvo encargo explícito de F20 (`ADR-014`).

Cada migración trae su/s test/s pgTAP en `supabase/tests/` (ver `supabase/tests/README.md`) y,
si cambia el esquema público, exige regenerar `src/lib/database.types.ts` con `pnpm db:types`.

## Aplicadas hasta ahora

| Archivo                                     | Tarea                  | Contenido                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ------------------------------------------- | ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `0001_extensions_and_schema_app.sql`        | DB-001                 | Extensión `btree_gist`; esquema `app`; `app.set_updated_at()`, `app.local_ts(date, time)`, `app.valid_weekdays(smallint[])`.                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `0002_enums.sql`                            | DB-002                 | Las 15 enumeraciones de `04_Modelo_de_Datos.md` sección 3.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `0003_profiles_roles_capabilities.sql`      | DB-003, DB-004, DB-005 | `profiles`, `user_roles`, `admin_capabilities` (RLS habilitada, sin políticas todavía); triggers `app.handle_new_user()` y `app.prevent_last_owner_removal()`; funciones de permisos (`jwt_roles`, `jwt_capabilities`, `has_role`, `is_admin`, `has_capability`, `require_role`, `require_admin`, `require_capability`); hook `app.custom_access_token_hook` y sus grants.                                                                                                                                                                       |
| `0004_company_holidays_security_events.sql` | DB-006                 | `company_settings` (singleton `id = 1`), `holidays`, `security_events` (RLS habilitada, sin políticas todavía); función `app.log_security_event(...)` (uso interno, `execute` revocado a `public`/`anon`/`authenticated`).                                                                                                                                                                                                                                                                                                                       |
| `0005_clients_sites.sql`                    | DB-007                 | `clients`, `client_contacts` (índice único parcial: un solo contacto principal por cliente), `sites` (nombre único por cliente, `unique (id, client_id)` para las FK compuestas de `services`/`shifts`); RLS habilitada, sin políticas todavía.                                                                                                                                                                                                                                                                                                  |
| `0006_employees.sql`                        | DB-008                 | Secuencia `employee_number_seq` y `employees`, `employee_client_permissions`, `employee_availability` (check `end_time > start_time`), `employee_leaves` (check `ends_on >= starts_on` y restricción de exclusión sobre el rango de fechas con `btree_gist`); RLS habilitada, sin políticas todavía.                                                                                                                                                                                                                                             |
| `0007_services_shifts_assignments.sql`      | DB-009                 | `services` (check de días con `app.valid_weekdays`); `shifts` (columnas generadas `starts_at`/`ends_at` con `app.local_ts`, ADR-019; unicidad parcial `(service_id, shift_date)`); `assignments` (franja propia opcional, columnas denormalizadas `shift_date`/`"window"` mantenidas por el trigger `app.sync_assignment_window`, restricción de exclusión `assignments_no_overlap` con `btree_gist`, unicidad parcial `(shift_id, employee_id)`); `app.current_employee_id()`, `app.shares_shift(uuid)`; RLS habilitada, sin políticas todavía. |
| `0008_checklists_tasks.sql`                 | DB-010                 | `checklist_templates` (unicidad parcial: una por cliente y una por sede, ADR-011), `checklist_template_items` (unicidad `(template_id, position)` deferrable), `shift_tasks` (copia del checklist en el turno); agrega la FK `shifts.checklist_template_id -> checklist_templates` pendiente desde 0007; RLS habilitada, sin políticas todavía.                                                                                                                                                                                                  |
| `0009_attendance.sql`                       | DB-011                 | `attendance_records` (`unique (assignment_id, kind)`, `reason` obligatorio si `source = admin`) y `attendance_notices` (`minutes_late`/`reason_code`/`reason_text` obligatorios según `kind`/`reason_code`); RLS habilitada, sin políticas todavía.                                                                                                                                                                                                                                                                                              |
| `0010_supervisions_ratings.sql`             | DB-012                 | `supervisions` (unicidad parcial `(shift_id, supervisor_id)` entre las no canceladas, P-086), `supervision_attendance`, `ratings` (`unique (supervision_id, assignment_id)`, check `score` 1..5), `rating_criteria`; `app.supervises_shift(uuid)` pendiente desde 0007; RLS habilitada, sin políticas todavía.                                                                                                                                                                                                                                   |
| `0011_views.sql`                            | DB-013                 | Las diez vistas de `04_Modelo_de_Datos.md` sección 4, todas `security_invoker = true`: `v_employees`, `v_shifts_board`, `v_assignments_board`, `v_my_day`, `v_supervisions_admin`, `v_my_supervisions`, `v_public_branding`, `v_people_basic`, `v_clients`, `v_search` (PROPUESTO). Agrega `app.today()` (auxiliar nuevo, fecha de hoy en Argentina) como base de las columnas derivadas que la necesitan.                                                                                                                                       |
| `0012_rls_policies.sql`                     | DB-014                 | Políticas RLS de `04_Modelo_de_Datos.md` sección 7.2, tabla por tabla, para las 25 tablas de negocio más las dos políticas de `anon` sobre `company_settings` (para que `v_public_branding` funcione bajo `security_invoker`). Ningún rol tiene política de `delete` (nada se borra físicamente). Ver `docs/database.md` para el detalle de decisiones (columnas por fila, guard de rol en las políticas "propias" de supervisión).                                                                                                              |
| `0013_rpc_users.sql`                        | DB-015                 | `set_user_roles`, `set_admin_capability`, `mark_changes_seen` (04 sección 9, 06_API.md sección 2.2), en el esquema `public` (PostgREST solo expone `public`/`graphql_public`). `security definer`, `search_path` fijo, `app.require_role`/`require_capability` primero, errores `P0001` en voseo. Eventos `roles_changed`/`capabilities_changed`.                                                                                                                                                                                                |
| `0014_storage_buckets.sql`                  | DB-016                 | Buckets `avatars` (`{profile_id}/{uuid}.jpg`, 2 MB, `image/jpeg`) y `branding` (`logo.{ext}`, 1 MB, PNG/JPEG/SVG/WebP), con las políticas de 04 sección 7.3 y ADR-016 sobre `storage.objects`.                                                                                                                                                                                                                                                                                                                                                   |
| `0015_indexes.sql`                          | DB-018                 | Sin DDL: los diecisiete índices de 04 sección 8 ya existían, creados en la migración de cada tabla (`0003` a `0010`). Solo documentación y pgTAP de verificación con `has_index`.                                                                                                                                                                                                                                                                                                                                                                |
| `0016_hardening.sql`                        | —                      | Cuatro endurecimientos: `search_path` fijo en las tres funciones de `0001` (`alter function`, sin recrearlas); check de formato de `clients.cuit`/`employees.cuil` (11 dígitos) y `employees.dni` (solo dígitos); `app.custom_access_token_hook` exige `profiles.is_active`/`deleted_at`; `app.sync_assignment_window` filtra `removed_at is null` al recalcular por cambio del turno.                                                                                                                                                           |
| `0017_grants.sql`                           | DB-017                 | Revoca todo a `anon`/`authenticated` sobre `public` y otorga desde cero: `anon` solo `v_public_branding` + columnas públicas de `company_settings`; `authenticated` `select` amplio + `insert`/`update`/`delete` donde corresponde; `execute` de las RPC de `0013`; grants de columna + trigger `app.enforce_profile_self_update_columns` en `profiles`; `alter default privileges` para tablas/vistas futuras. Ver `docs/database.md`.                                                                                                          |
